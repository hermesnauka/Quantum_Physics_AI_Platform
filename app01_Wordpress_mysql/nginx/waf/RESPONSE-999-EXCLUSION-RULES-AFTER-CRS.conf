# AS-03 WAF (ModSecurity/CRS) response-side exclusions, evaluated after the
# core rule set runs. Mounted at
# /etc/modsecurity.d/owasp-crs/rules/RESPONSE-999-EXCLUSION-RULES-AFTER-CRS.conf.
# See REQUEST-900-EXCLUSION-RULES-BEFORE-CRS.conf in this same directory for
# why it's deliberately empty for now.
